<% if @reservations.blank ? %>
<p>Brak rezerwacji.< / p>
< % else%>
<p>Oto sa rezerwacje< / p>

<ul id = "reservations">
< % @reservations.each do | c | %>
<li> < %= link_to c.parking_spot_id, { :action = > 'show', :id = > c.id } - % > < / li>
< % end%>
< / ul>

< % end%>